{
  "name": "room-next",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node scripts/check.mjs",
    "dry-run": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "agents": "0.21.0"
  },
  "devDependencies": {
    "wrangler": "4.125.0"
  }
}
