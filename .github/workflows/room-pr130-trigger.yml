name: Room PR130 trigger

on:
  push:
    branches: [fix/room-single-prior-phrase-echo-20260821]
    paths:
      - '.github/workflows/room-pr130-trigger.yml'

jobs:
  marker:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - run: python3 scripts/room_consensus_echo_sim.py
